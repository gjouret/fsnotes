A short paragraph before the first fenced block.

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)"
}
```

A short paragraph between the two fenced blocks.

```
plain text with no language tag
line two of the plain block
line three closes the block
```

A final paragraph after both fences.
