The quick brown fox jumps over the lazy dog.
This sentence has **bold** text and *italic* text.
Inline `code` is also present here.

A second paragraph includes a [link to somewhere](https://example.com).
It continues with an escaped asterisk \* which should survive round-trip.
Mixing **bold** with *italic* in the same line is common prose.

The final paragraph is intentionally short.
It has a trailing note with `code` near the end.
Plain prose dominates this corpus file.
